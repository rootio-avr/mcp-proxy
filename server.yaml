name: rootio-mcp-proxy
image: mcp/rootio-mcp-proxy
type: server
meta:
  category: security
about:
  title: Root.io Vulnerability Remediation
  description: MCP server for Root.io that provides container image vulnerability scanning and remediation capabilities
source:
  project: https://github.com/rootio-avr/mcp-proxy
config:
  env:
  - name: API_ACCESS_TOKEN
    description: Access token for authentication
    required: false